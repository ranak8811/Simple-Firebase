/**
 * 1. first install firebase: npm install firebase
 * 2. console.firebase.google.com: create a project
 * 3. Register project (I did using the same name before)
 * 4. get a firebase config and do not save config on the client side
 * 5. create auth where it will be added
 * 6. export auth from firebase init file
 * 7. in the console authentication > started > enable Google Provider authentication
 */
